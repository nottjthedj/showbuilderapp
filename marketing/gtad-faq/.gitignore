# Regenerated by build.py — the PNGs in png/ are the tracked deliverable.
html/

# Fetched by setup.sh (fonts are OFL, redistributable, but not vendored here).
fonts/
node_modules/
package.json
package-lock.json
